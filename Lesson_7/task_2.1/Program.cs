// Задача 2.1:Напишите программу вычисления функции Аккермана с помощью рекурсии. 
// Даны два неотрицательных числа m и n.
// Не смог решить

// int Ankerman (int n, int m)
// {
//         if (n < 0 || m < 0) throw new ArgumentOutOfRangeException();
//         if (m == 0) 
//           return (n - 1, 1);
//         if (m == 0) 
//           return ( n - 1, m);
//           return ( n - 1, (n, m - 1));
// }
// int n = 5;
// int m = 1;
// Ankerman(n, m);